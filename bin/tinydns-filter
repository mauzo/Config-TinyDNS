#!/usr/bin/perl

use warnings;
use strict;

use Config::TinyDNS qw/:ALL/;
use File::Slurp     qw/slurp/;

my @filters = map { /^(.*?)=(.*)/ ? [$1, split /,/, $2] : $_ } @ARGV;
print process_config scalar slurp(\*STDIN), @filters;
